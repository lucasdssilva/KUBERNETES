services:
  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin

      GF_AUTH_GENERIC_OAUTH_ENABLED: "true"
      GF_AUTH_GENERIC_OAUTH_NAME: Keycloak
      GF_AUTH_GENERIC_OAUTH_ALLOW_SIGN_UP: "true"

      GF_AUTH_GENERIC_OAUTH_CLIENT_ID: teste-1
      GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET: rOehl2v8CyJvzqBY30UBGisBfWYEFTEC

      GF_AUTH_GENERIC_OAUTH_SCOPES: openid profile email

      GF_AUTH_GENERIC_OAUTH_AUTH_URL: https://http://keycloak.local//realms/teste/protocol/openid-connect/auth
      GF_AUTH_GENERIC_OAUTH_TOKEN_URL: https://http://keycloak.local//realms/teste/protocol/openid-connect/token
      GF_AUTH_GENERIC_OAUTH_API_URL: https://http://keycloak.local//realms/teste/protocol/openid-connect/userinfo

      GF_SERVER_ROOT_URL: http://localhost:3000

      GF_AUTH_DISABLE_LOGIN_FORM: "true"

      # docker compose -f grafana-front.yaml up -d